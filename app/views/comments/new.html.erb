<h1>New Comment</h1>
<% @comment.update(gossip_id: params[:number]) %>  

<%= form_for @comment, url: {action: "create"} do |f| %>
  </div>
  Content:
  <div class="form_group">
    <%= f.text_area :content %>
  </div>

  <div class="form_group">
    <%= params[:number]%> <br>
    Enter the number Above: or a specific one if you want to comment another gossip <br>
    <%= f.number_field :gossip_id%>
  </div>
  <div class="form_group">
    <%= f.submit "Create" %>
  </div>
<% end %>


<%= link_to 'Back', gossip_index_path %>
