<h1>Gossip List</h1>

<% @gossips.each do |gossip| %>
  <h2><%= gossip.title %> </h2> <br>
  <h5><%= gossip.content %> </h5> <br>
  <p>
    <a href="<%= gossip_path(gossip.id) %>" >En savoir plus</a>
  </p>
  
<% end %>


