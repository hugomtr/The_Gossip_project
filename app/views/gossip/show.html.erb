<h1> <%= @gossip.title %></h1>

<p> <%= @gossip.content %> </p>

<a href="<%=gossip_index_path%>"> Retour à la liste des potins</a>

<p> 
<a href="<%=user_path(@gossip.user_id)%>">L'Auteur</a>
<%= User.find(@gossip.user_id).first_name %> 
</p>

<p> 
<a href="<%=city_path(User.find(@gossip.user_id).city_id) %>">La ville de l'auteur</a> 
<%= City.find(User.find(@gossip.user_id).city_id).name %> 
</p>
<p>
<%= link_to 'Edit', edit_gossip_path(@gossip.id) %>
</p>
